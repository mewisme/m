# Runtime assets

Source tree for Node loader, preload, and worker JavaScript embedded into Mew
via `go:embed` (see `internal/runtime/assets` and MVP 0050).

JavaScript here is limited to Node extension surfaces. See
[`docs/architecture/node-augmentation.md`](../docs/architecture/node-augmentation.md).
