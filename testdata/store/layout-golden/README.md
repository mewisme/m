# Golden store layout (MVP 0018)

Sample `index.json` and directory shape:

```text
<store>/
  index.json
  packages/
    sha256/
      <hex>/
        package.json
        .mew-package-integrity
  .staging/
```

See `index.json` in this directory for a minimal document sample.
