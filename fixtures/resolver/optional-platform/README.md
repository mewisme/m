# Optional / platform fixture

- `package.json` — root `optionalDependencies` with darwin-only `opt-darwin`
- `package-transitive.json` — `opt-parent` with failing optional transitive dep
- `registry/` — local packuments with `os` / `cpu` metadata
