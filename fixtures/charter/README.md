# Charter fixtures

Minimal projects for charter and migration documentation tests.

| Fixture | Lockfile | Tests |
|---|---|---|
| `npm-app` | `package-lock.json` | npm identity preservation wording |
| `pnpm-app` | `pnpm-lock.yaml` | pnpm identity preservation wording |
| `nub-app` | `nub.lock` | Nub identity preservation wording |
| `empty` | (none) | greenfield `m.lock` default |

These fixtures are intentionally small. They are not conformance lockfile goldens.
