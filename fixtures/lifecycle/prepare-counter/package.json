{
  "name": "lifecycle-counter",
  "version": "1.0.0",
  "scripts": {
    "prepare": "node -e \"var fs=require('fs'),p=require('path'),f=p.join(__dirname,'.run-count');var n=fs.existsSync(f)?parseInt(fs.readFileSync(f,'utf8'),10):0;fs.writeFileSync(f,String(n+1))\""
  }
}
