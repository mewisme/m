# Unified execution fixtures (MVP 0045)

| Fixture | Purpose |
|---------|---------|
| `project/` | Baseline local importer execution |
| `dlx/` | Ephemeral package set for mx/envexec DLX |

Snapshot and capsule fixtures are generated in integration tests using testkit helpers.
